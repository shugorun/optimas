# Gold データ仕様

評価専用の凍結コーパスと、その上の「正解」を制約集合として定義する（[ADR-0007](docs/adr/0007-constraint-based-gold.md)）。これに対し [RUBRIC.md](RUBRIC.md) の条件1（ノート単位）・条件2（グラフ）を測る。

## 凍結コーパス（[CONTEXT.md](CONTEXT.md): 凍結コーパス）

- 日記・AIとの会話・論文の **3ソース混在**スナップショット（異種ソース間リンクこそ評価対象）。
- 規模の目安：**80〜120 atom**。トピックはある程度まとまりがある方がリンク密度が上がる。

## Gold の構成要素

### 1. 抽出 Gold（ソース passage ごと）

- 各 passage → **gold 命題の集合**。各命題：`{id, 代表パラフレーズ(参照用・文字列一致には使わない), provenance(種別/日付/生テキスト)}`
- **ゼロ命題負例**：命題核の無いエピソード（「今日は疲れた」等）→「atom なし」が正解。**必ず含める**（LLM の命題捏造を検出）。
- **intra-source リンク**：同 passage の命題間関係（無型。食い違いは contradicts 型）。決定論的に列挙。

### 2. cross-source リンク Gold（プーリングで作成）

- 被テスト系＋強リトリーバルのベースライン1〜2本に各ノートの候補上位 k≈10 を出させ、**プール**。
- プール候補を人間が **related y/n** 判定。さらに**プール外ランダムサンプル（〜200件）**も判定し recall の分母を推定。
- 正例リンク目標：**100〜200 本**（F1 を ±5% 程度で測れる量）。

### 3. 矛盾 Gold

- **contradiction ペア**：`{命題A, 命題B, 食い違いの理由}`。異種ソース間（例：日記の誤解 ⨯ 論文の主張）を意図的に含める。

### 4. merge Gold

- **命題同一クラスタ**：複数ソース由来で1ノートに merge すべき命題の集合（provenance が累積されるべき先）。別命題の誤 merge も負例として持つ。

### 5. ルーブリック B群 人間ラベル（ジャッジ較正用・[ADR-0004](docs/adr/0004-calibrated-llm-judge-for-judgment-items.md)）

- 較正セットのノートについて **A1/A2/C1/R1/W1/M1 の人間 y/n**。LLM ジャッジの y/n と突き合わせ κ を測る。

## 粒度の固定

granularity は A1（独立参照テスト）で一意に決定。phrasing は自由、粒度は固定。Gold アノテータはこのルールを一貫適用する。

## 評価手順

1. **アライン**：出力ノート → Gold 命題（較正済みマッチャ）。
2. **ルーブリック条件1**：A群を決定論ルール、B群を較正ジャッジで判定。全ノート全項目 Y か。
3. **G1 recall / G2 precision**：intra は決定論で 100% 要求。cross は Gold ペアへの recall と precision（過剰リンクは precision で落ちる）。
4. **G3 merge**：命題同一が1ノートに集約され、別命題が誤 merge されていないか。
5. **矛盾 recall**：contradiction ペアが矛盾リンクで捕捉されているか。

## メトリクスと閾値

| 対象 | 指標 | 合格条件 |
|---|---|---|
| ルーブリック | 全ノート全項目 Y | 二値（全クリアのみ合格） |
| cross リンク | F1（precision フロア付き） | 過剰リンクを precision で禁止 |
| ジャッジ較正 | Cohen's κ | > 0.8（超のときのみジャッジ信用） |
| retrieval recall | cross リンク recall の上限 | 別途測定（リンク recall の天井） |

## 規模と工数

- 80〜120 atom、プーリングで **800〜1,200 のペア二値判定**＋プール外サンプル200。正例 100〜200。
- 工数：**週末1回（実 3〜5時間）**。LLM に仮ラベル → 人間が反転、で 3〜5倍短縮。
- バイアス対策：仮ラベル LLM は**被テスト系と別モデル/別プロンプト**。**正例は全目視、負例はサンプル目視**。
- この Gold は固定資産。プロンプト／アルゴリズムを何度チューニングしても再利用できる。
