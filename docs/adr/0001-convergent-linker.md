# リンカは収束的（順序非依存）にする

取り込みは高頻度・増分的で、ノートは到着順に関わらず最終的に同じリンクグラフへ整定すべきである。そこで、新ノート追加のたびにリトリーバルを再走させ既存ノート側にもリンクを張り直す「収束的リンカ」を採用する。

挿入時に1回だけリンクして二度と見直さない方式の方が安価だが、それだと最終グラフが入力の到着順に依存し、評価 Gold を各時点ごとに用意する必要が生じてアノテーション量が `ステップ数 × ペア数` に爆発する。収束性により最終状態の Gold 1枚だけで全体をテストでき、これが採用理由。
